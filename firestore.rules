rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /analytics/{doc=**} { allow read, write: if false; }
    match /cache/{doc=**} { allow read: if request.auth != null; allow write: if false; }
    match /rate_limits/{uid} { allow read, write: if false; }
  }
}